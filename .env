# vCenter connection
VCENTER_URL=
VCENTER_USER=administrator@vsphere.local
VCENTER_PASS=

# Optional runtime
VCENTER_LISTEN=:8081
VCENTER_SECURE_TLS=false   # true to verify ESXi certs

# Note: shell env overrides .env
